name: Simple CI Dev

on:
  push:
    branches: [develop]

jobs:
  build:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v3
      - run: echo "Hello, GitHub Actions for Development!"