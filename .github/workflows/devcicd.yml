name: Simple CI Dev

on:
  push:
    branches: [temp]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: echo "Hello, GitHub Actions for Development!"
